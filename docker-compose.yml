services:
  web:
    image: nginx:latest
    restart: always
    ports:
      - 80:80
    volumes:
      - ./data:/data
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/conf.d:/etc/nginx/conf.d
